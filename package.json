{
  "dependencies": {
    "husky": "^3.0.3",
    "lint-staged": "^9.2.1"
  },
  "scripts": {
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.{rb}|Gemfile": "bundle exec rubocop --safe-auto-correct --force-exclusion"
  }
}
